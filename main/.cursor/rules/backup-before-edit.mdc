---
description: 修改现有文件前先 git 备份或本地备份
alwaysApply: true
---

# 修改前先备份

在**编辑、覆盖或删除**仓库内已有文件之前，必须先保留旧版本。未完成备份前，不要开始改文件。

## 优先顺序

1. **Git 备份（优先）**  
   工作区在 git 仓库内时，对即将修改的文件先做版本留存，任选其一：
   - `git stash push -m "backup before: <简要说明>" -- <文件路径...>`
   - 或 `git add <文件路径...>` 后执行仅含这些文件的备份提交（提交信息注明 `backup before edit`；**未经用户明确要求，不要 push**）

2. **本地文件备份（无 git 或 git 不可用时）**  
   将待改文件的当前内容复制到 `.history/`，命名与项目已有 Local History 一致：
   - 目标路径：`.history/<相对目录>/<文件名不含扩展名>_<yyyyMMddHHmmss>.<扩展名>`
   - 相对路径以工作区根目录（`main/`）为基准，不含 `main/` 前缀
   - 示例：`APP/Driver/src/GPRS.c` → `.history/APP/Driver/src/GPRS_20260629170714.c`
   - 保持原目录结构；若 `.history/` 或其子目录不存在则创建
   - `.history/` 已在 `.gitignore` 中，仅作本地快照，不要提交

## 执行时机

- 每次新任务中**第一次**改动某文件前，对该文件做一次备份。
- 同一对话里若已备份过且该文件未被他人/外部改动，可不再重复备份。
- 仅新建文件、不改已有文件时，无需备份。

## 禁止与注意

- 不要用破坏性 git 命令（如 `reset --hard`、强制覆盖远程）代替备份。
- 备份失败时，先告知用户并询问是否继续，不要静默修改。
- 批量改动前，可一次性备份全部目标文件路径。
- 本规则与「仅在被要求时 commit」并存：备份提交仅用于改前快照，不等同于功能交付提交。
- 不再使用 `backup/YYYYMMDD_HHMM/` 目录；旧备份可保留，新备份一律写入 `.history/`。

## 示例

```bash
# 单文件 git stash 备份
git stash push -m "backup before: fix eth network reset" -- APP/Task/src/eth.c
```

```powershell
# 单文件本地备份（PowerShell 示例）
$ts = Get-Date -Format "yyyyMMddHHmmss"
$src = "APP\Task\src\eth.c"
$dir = Split-Path $src -Parent
$name = [System.IO.Path]::GetFileNameWithoutExtension($src)
$ext = [System.IO.Path]::GetExtension($src)
$dst = ".history\$dir\${name}_$ts$ext"
New-Item -ItemType Directory -Force -Path (Split-Path $dst) | Out-Null
Copy-Item $src $dst -Force
```

```powershell
# 多文件本地备份
$ts = Get-Date -Format "yyyyMMddHHmmss"
$files = @("APP\Task\src\eth.c", "APP\Driver\src\GPRS.c")
foreach ($src in $files) {
    $dir = Split-Path $src -Parent
    $name = [System.IO.Path]::GetFileNameWithoutExtension($src)
    $ext = [System.IO.Path]::GetExtension($src)
    $dst = ".history\$dir\${name}_$ts$ext"
    New-Item -ItemType Directory -Force -Path (Split-Path $dst) | Out-Null
    Copy-Item $src $dst -Force
}
```
