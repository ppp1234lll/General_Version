---
description: 项目源码文件使用 GBK 编码
globs: "**/*.{c,h}"
alwaysApply: true
---

# 文件编码：GBK

本工程（嵌入式 C 源码，含 Keil 工程）统一使用 **GBK** 编码。

## 编辑与新建文件时

- 新建或修改 `.c`、`.h` 等源码文件时，保持 **GBK** 编码。
- 中文注释、字符串字面量按 GBK 保存，避免在 GBK 环境下出现乱码。
- 与现有文件编码保持一致；不要无意将 GBK 文件保存为 UTF-8。

## 注释与文档

- 中文注释使用简体，风格与 `log.c`、`GPRS.c` 等现有文件一致。
- 避免在 GBK 文件中混入仅 UTF-8 可正确显示的特殊字符。

## 注意

- 若编辑器默认 UTF-8，保存前确认编码为 GBK。
- 批量改码或修复乱码时，先确认原文件实际编码再转换。
